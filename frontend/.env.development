# Development-only auto-login credentials
# These variables are ONLY available when running `npm run dev`
# They are NOT included in production builds (`npm run build`)
VITE_DEV_EMAIL=dev@workspace.local
VITE_DEV_PASSWORD=password123
VITE_API_URL=http://127.0.0.1:3000
